# WIP
version: '3'
services:
    audio_auth:
        build: ./audio_authentication

    face_auth:
        build: ./face_authentication
        privileged: true
        devices:
        - "/dev/video1:/dev/video1"
        
    user:
        build: ./user_interface
        depends_on: 
        - audio
        - face
